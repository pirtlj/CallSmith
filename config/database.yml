# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
    adapter: sqlite3
    database: db/development.sqlite3
    
    #host: localhost
    #adapter: mysql
    #database: callsmith_development
    #port: 3306
    #username: root

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    host: localhost
    adapter: mysql
    database: callsmith_test
    port: 3306
    username: root

production:
    host: localhost
    adapter: mysql
    database: callsmith_production
    port: 3306
    username: root
